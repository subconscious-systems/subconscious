VITE_CONVEX_URL=

# Setup:
# 1. npm install
# 2. npx convex dev (keep running - generates convex/_generated/)
# 3. Copy URL from step 2 to VITE_CONVEX_URL above
# 4. In Convex Dashboard > Settings > Environment Variables, set:
#    SUBCONSCIOUS_API_KEY = your key from subconscious.ai
#    CONVEX_URL = same URL as VITE_CONVEX_URL
# 5. npm run dev (in new terminal)
